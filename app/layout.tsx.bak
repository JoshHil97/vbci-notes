import "./globals.css";

export const metadata = {
  title: "VBCI Notes",
  description: "Weekly sermon notes, written clearly and preserved for the church family.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
